<Window x:Class="AttentionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Attention Dashboard" Height="844" Width="390" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Header -->
            <RowDefinition Height="60"/>
            <!-- Lesson Name -->
            <RowDefinition Height="300"/>
            <!-- Attention Graph -->
            <RowDefinition Height="200"/>
            <!-- Classroom Map -->
            <RowDefinition Height="*"/>
            <!-- Suggestions -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Real-Time Analysis" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- Lesson Name + Time -->
        <TextBlock Grid.Row="1" Text="Math 101 | 8:15 - 9:50" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- Attention Graph -->
        <lvc:CartesianChart x:Name="AttentionChart" Grid.Row="2"/>

        <!-- Classroom Map -->
        <Canvas x:Name="ClassroomMap" Grid.Row="3" Background="LightGray" Height="200" Width="360">
            <!-- Example students -->
            <Ellipse Width="30" Height="30" Fill="Green" Canvas.Left="50" Canvas.Top="30"/>
            <Ellipse Width="30" Height="30" Fill="Yellow" Canvas.Left="120" Canvas.Top="30"/>
            <Ellipse Width="30" Height="30" Fill="Red" Canvas.Left="190" Canvas.Top="30"/>
            <Ellipse Width="30" Height="30" Fill="Green" Canvas.Left="50" Canvas.Top="100"/>
            <Ellipse Width="30" Height="30" Fill="Yellow" Canvas.Left="120" Canvas.Top="100"/>
            <Ellipse Width="30" Height="30" Fill="Red" Canvas.Left="190" Canvas.Top="100"/>
        </Canvas>

        <!-- Suggestions -->
        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="Suggestions to Improve the Lesson:" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
            <TextBlock Text="• Ask students questions more often" FontSize="13" Margin="5,2,0,0"/>
            <TextBlock Text="• Shorter explanation segments" FontSize="13" Margin="5,2,0,0"/>
            <TextBlock Text="• Move closer to distracted students" FontSize="13" Margin="5,2,0,0"/>
        </StackPanel>
    </Grid>
</Window>
